# Asterisk config

Sample config files for PJSIP-based intercoms.

- `pjsip.conf` — SIP endpoints for intercoms and mobile clients
- `extensions.conf` — dialplan for incoming calls
- `rtp.conf` — RTP port range

Volumes mounted read-only from `infra/asterisk/`.
